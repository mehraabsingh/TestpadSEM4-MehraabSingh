read -p "Enter a number: " number
sum=0
if ! [[ "$number" =~ ^[0-9]+$ ]]; then
    echo "Please enter a valid positive integer."
    exit 1
fi
for (( i=0; i<${#number}; i++ )); do
    digit=${number:i:1}  
    sum=$((sum + digit))  
done
echo " $sum"
